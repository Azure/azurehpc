trigger:
  batch: true
  branches:
    include : 
    - azure-pipelines
  paths:
    include:
      - "*.yml"


pool: Default

steps:
  - script: |
      pwd
      ls -al
    displayName: 'Run a multi-line script'
